---
date: 2026-04-20 21:31 SEAST
session: oracle-dashboard — VPS bridge + hotkeys
---

## Summary

- **oracle-dashboard เปิด tmux bridge** — เพิ่ม `TMUX_BRIDGE_ENABLED=1` + SSH config ไป Ariadne (VPS 72.60.77.195)
- **แก้ bug 3 จุดใน ssh-exec.ts + tmux-bridge.ts** — Windows OpenSSH ไม่รองรับ `--` separator, `|||` ถูก remote shell แปลเป็น pipe, `#{pane_id}` ออกเป็น `%0` แต่ parser ทำ `Number("%0")` = NaN
- **ดึง VPS oracle-fleet 11 panes ได้สำเร็จ** — ariadne, pythia, mnemosyne, prometheus, athena, relay, fleet, ssh-ariadne, git-log, mission-ctrl, maw-studio
- **Hotkeys เปลี่ยนเป็น Shift+1-9** — จาก Ctrl+Shift+1-9 (3 ปุ่ม) เป็น 2 ปุ่ม ใช้ `e.code` แทน `e.key`
- **Talk to Mother** — ส่งข้อความผ่าน Slack relay แจ้งสถานะ oracle-dashboard สำเร็จ

## Learned

`#{pane_id}` ใน tmux ออกมาเป็น `%0` ไม่ใช่ `0` — ต้อง strip `%` prefix ก่อน parse เป็น number

## Next

- ทดสอบ Shift+1-9 จริงในเบราว์เซอร์
- เพิ่ม Mother เข้า ψ/contacts.json
- ทดสอบส่ง command จริงไปยัง VPS pane ผ่าน Shift+Alt+Enter
